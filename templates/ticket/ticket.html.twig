{% extends 'base.html.twig' %}
{% block title %}Hello      {% endblock %}

{% block body %}


    <h1 class="text-primary">Choisissez la date de votre visite </h1>

    {# render the task's only field: description #}

  
{{ form_start(form) }}
    {# render the task's only field: description #}
<h3> Choisissez la date de votre visite </h3>

    
    
    {{ form_widget(form.reservationDate) }}
    <h3>Tickets</h3>
    <ul class="tickets" data-prototype="{{ form_widget(form.tickets.vars.prototype.firstName)|e('html_attr') }}"></ul>
   
    
        {# iterate over each existing ticket and render its only field: name #}
        {# for ticket in tickets#}

        {# endfor #}
{% endblock %}